# Copia questo file in ".env" (già ignorato da git) e inserisci la tua chiave.
# Genera una chiave nuova su https://console.groq.com/keys DOPO aver revocato
# quella vecchia che era stata committata per errore nel codice.
GROQ_API_KEY=
